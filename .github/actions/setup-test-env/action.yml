name: Setup dotnet testing environment

runs:
  using: composite
  steps:
    - run: git submodule update --init --recursive
      shell: bash
    - uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 9.0.x
    - run: dotnet build -c DebugOpt
      shell: bash