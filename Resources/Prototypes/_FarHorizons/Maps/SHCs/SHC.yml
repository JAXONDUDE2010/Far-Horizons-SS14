- type: gameMap
  id: SHC
  isGrid: true # Did you know that centcomm is the only "game map" that isn't actually a map? Send help.
  mapName: 'Syndicate High Command'
  mapPath: /Maps/_FarHorizons/SHCs/shc.yml
  minPlayers: 10
  stations:
    centcomm:
      stationProto: SyndicateHighCommand
      components:
        - type: StationNameSetup
          mapNameTemplate: '{0} Syndicate High Command {1}'
          nameGenerator:
            !type:NanotrasenNameGenerator
            prefixCreator: 'TG'
